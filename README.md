# Academ - Static Website

Статический веб-сайт для поиска отелей.

## Структура файлов

```
Academ/
├── index.html          # Главная страница поиска
├── search.html         # Страница результатов поиска
├── styles.css          # Все стили проекта
├── README.md           # Эта инструкция
├── FILES_LIST.txt      # Список всех файлов
└── assets/             # Ресурсы
    ├── logo.svg        # Логотип
    ├── map.png         # Изображение карты
    ├── *.png           # Изображения отелей
    ├── Fonts/          # Локальные шрифты
    │   ├── YS Text/    # Шрифт YS Text (все веса)
    │   └── YS Display/ # Шрифт YS Display (все веса)
    └── icons/          # SVG иконки
        ├── 12/         # Иконки 12px
        ├── 16/         # Иконки 16px
        ├── 20/         # Иконки 20px
        └── 24/         # Иконки 24px
```

## Сборка статического сайта

### Команда buildstatic

Для сборки статического сайта со всеми необходимыми файлами (включая локальные шрифты) используйте команду:

```bash
npm run buildstatic
```

### Что делает команда:

1. **Копирует шрифты** из `assets/Fonts/` в `Academ/assets/Fonts/`
   - YS Text (Regular, Medium, Bold, Heavy, Light + Italic варианты)
   - YS Display (Regular, Medium, Bold, Heavy, Light)

2. **Создает ZIP архив** для развертывания:
   - `Academ-static-website-YYYY-MM-DDTHH-MM-SS.zip` (с временной меткой)
   - `Academ-static-website.zip` (последняя версия)

3. **Выводит информацию** о созданных файлах

### Пример использования:

```bash
# Перейти в корень проекта
cd "/Users/yuriyshiryaev/Documents/Design Travel Cursor react"

# Запустить сборку
npm run buildstatic

# Результат:
# 🚀 Building static website...
# 📁 Copying fonts...
#   ✓ YS Text fonts copied
#   ✓ YS Display fonts copied
# 📦 Creating ZIP archive...
#   ✓ Created Academ-static-website-2026-02-06T21-37-09.zip
# ✅ Static website build complete!
```

## Развертывание на сервере

### Вариант 1: Использование готового архива (рекомендуется)

1. **Запустите команду сборки:**
   ```bash
   npm run buildstatic
   ```

2. **Найдите созданный архив:**
   - `Academ-static-website.zip` в корне проекта

3. **Распакуйте архив** на вашем компьютере

4. **Загрузите все файлы** из папки `Academ/` на ваш веб-сервер:
   - Используйте FTP, SFTP, или веб-интерфейс хостинга
   - Загрузите всю папку `Academ/` со всем содержимым

5. **Убедитесь, что структура папок сохранена:**
   - Особенно важно сохранить `assets/Fonts/` и `assets/icons/`
   - Все пути к ресурсам относительные

6. **Откройте сайт:**
   - Главная страница: `https://yourdomain.com/Academ/index.html`
   - Или настройте веб-сервер для обслуживания из папки `Academ/`

### Вариант 2: Прямая загрузка файлов

1. **Убедитесь, что шрифты скопированы:**
   ```bash
   npm run buildstatic
   ```

2. **Загрузите все файлы** из папки `Academ/` на ваш веб-сервер:
   - Используйте FTP/SFTP клиент или веб-интерфейс
   - Загрузите всю структуру папок

3. **Проверьте структуру** на сервере:
   - `Academ/assets/Fonts/YS Text/` должен содержать файлы `.ttf`
   - `Academ/assets/Fonts/YS Display/` должен содержать файлы `.ttf`
   - `Academ/assets/icons/` должен содержать все SVG файлы

4. **Откройте `index.html`** в браузере

## Требования

- **Современный веб-браузер** (Chrome, Firefox, Safari, Edge)
- **Веб-сервер** для статических файлов (Apache, Nginx, или любой другой)
- **Node.js и npm** (только для сборки, не требуется на сервере)

## Особенности

- ✅ **Полностью статический сайт** - не требует серверной логики
- ✅ **Локальные шрифты** - YS Text и YS Display подключены локально, не требует CDN
- ✅ **Адаптивный дизайн** - работает на мобильных и десктопах
- ✅ **Календарь выбора дат** - интерактивный календарь для выбора дат заезда/выезда
- ✅ **Фильтры поиска отелей** - фильтры по цене, звездности, удобствам
- ✅ **Переключение валют** - поддержка рублей (₽) и тенге (₸)
- ✅ **Мобильная версия** - адаптивная верстка для мобильных устройств

## Поддерживаемые страницы

- **`/index.html`** - Главная страница с формой поиска отелей
- **`/search.html`** - Страница результатов поиска с фильтрами

## Технические детали

### Шрифты

- **YS Text** - основной шрифт для текста
  - Веса: 300 (Light), 400 (Regular), 500 (Medium), 700 (Bold), 900 (Heavy)
  - Поддержка курсива для Regular и Medium
  - Подключены через `@font-face` в `styles.css`
  - Файлы находятся в `assets/Fonts/YS Text/`

- **YS Display** - шрифт для заголовков
  - Веса: 300 (Light), 400 (Regular), 500 (Medium), 700 (Bold), 900 (Heavy)
  - Подключены через `@font-face` в `styles.css`
  - Файлы находятся в `assets/Fonts/YS Display/`

### Ресурсы

- **Все пути к ресурсам относительные** - работают при правильной структуре папок
- **JavaScript встроен** в HTML файлы - не требует отдельных JS файлов
- **CSS в отдельном файле** `styles.css` - подключен через `<link>`
- **Иконки в формате SVG** - векторные, масштабируемые
- **Изображения в формате PNG** - растровые изображения отелей и карт

### Структура CSS

- CSS переменные для цветов, размеров, отступов
- Адаптивные медиа-запросы для мобильных устройств
- Стили компонентов следуют дизайн-системе Yandex Travel

## Устранение неполадок

### Шрифты не загружаются

1. Проверьте, что папка `assets/Fonts/` существует на сервере
2. Убедитесь, что файлы `.ttf` загружены в правильные папки
3. Проверьте права доступа к файлам на сервере
4. Откройте консоль браузера (F12) для проверки ошибок загрузки

### Изображения не отображаются

1. Проверьте, что папка `assets/` загружена полностью
2. Убедитесь, что пути к изображениям правильные (относительные)
3. Проверьте права доступа к файлам

### Иконки не отображаются

1. Проверьте, что папка `assets/icons/` загружена со всеми подпапками
2. Убедитесь, что структура `12/`, `16/`, `20/`, `24/` сохранена
3. Проверьте пути к иконкам в HTML файлах

## Команды проекта

```bash
# Сборка статического сайта
npm run buildstatic

# Разработка (React приложение)
npm run dev

# Сборка React приложения
npm run build

# Просмотр собранного React приложения
npm run preview
```

## Лицензия

Проект использует шрифты YS Text и YS Display от Yandex. Использование шрифтов регулируется лицензией Yandex.
